{% if db_loaded == false %}
    <i id="db_online_state" class="bi-lightbulb-fill" data-state="offline"></i>
{% else %}
    <i id="db_online_state" class="bi-lightbulb-fill" data-state="online"></i>
{% endif %}